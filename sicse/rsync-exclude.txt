# Prevent the Let's Encrypt data being deleted.
/web/.well-known/

# Prevent uploading settings files.
/web/sites/*/*settings*.php

# Exclude the "files" directory.
/web/sites/*/files/

# Prevent uploading example settings/configuration files.
/web/sites/example*.php

# Exclude the configuration file for Windows servers.
/web/web.config

# Prevent uploading the file used by PHP's internal webserver.
/web/.ht.router.php

# Prevent the .gitignore example file from being uploaded.
/web/example.gitignore

# No need to copy JavaScript building files.
/web/core/.yarn
/web/core/node_modules/

# Since we build the site in a different environment, no need to copy the patches to the hosting environment.
patches*.json
/config/patches/

# No need to copy the files used to create the scaffold files.
/config/scaffold/

# Drush can be used in the hosting environment, but there is no need to access other sites (such as @acc or @prod).
/drush/sites/

# The configuration files for the development environment shouldn't be uploaded.
/config/splits/dev/
/web/sites/development.services.yml

# Since the .htaccess file in the tmp folder is automatically created, no need to upload this file.
/tmp/

# Ignore files generated by PhpStorm.
/.idea/

# Don't copy any of the files used by the DDEV stack.
/.ddev/

# Don't sync .env variables as they are environment specific.
.env*

# Composer configuration files shouldn't be uploaded. Except for the one in the doc-root, otherwise, Drush isn't
# working correctly.
+ /composer.json
composer.json
composer.lock

# Node configuration files shouldn't be uploaded.
package*.json
yarn.lock

# Prevent uploading IDE editor configuration files.
.csslintrc
.editorconfig
.eslint*
.prettier*
.stylelint*

# Rector configuration is only needed for development.
/rector.php

# Exclude continuous integration configuration files.
.tugboat
drupalci.yml

# Exclude PHPUnit and PHP_CodeSniffer configuration files.
phpunit.xml*
phpcs.xml*

# Prevent uploading Twig CS fixer cache files.
.twig-cs-fixer.cache

# Prevent uploading directories containing test files and/or results.
*/tests/
/web/sites/simpletest/

# Prevent uploading theme source and compiler configuration files.
/web/themes/**/node_modules/
*.pcss.css
*.es6.js
tailwind.config.js

# Exclude all Git related directories and files.
.git*

# Exclude all files containing information about version changes.
CHANGELOG
CHANGELOG.md
CHANGELOG.txt
CHANGES
CHANGES.md
CHANGES.txt

# Exclude all files containing the code of conduct.
CODE_OF_CONDUCT
CODE_OF_CONDUCT.md
CODE_OF_CONDUCT.txt

# Exclude all files containing a contribution notes.
CONTRIBUTING
CONTRIBUTING.md
CONTRIBUTING.txt

# Exclude all files containing copyright information.
COPYRIGHT
COPYRIGHT.md
COPYRIGHT.txt

# Exclude all files containing installation instructions.
INSTALL
INSTALL.md
INSTALL*.txt

# Exclude all files containing license information.
LICENSE
LICENSE.md
LICENSE.txt

# Exclude all files denoting maintainer information.
MAINTAINERS
MAINTAINERS.md
MAINTAINERS.txt

# Exclude all files containing information on patches that were applied.
PATCHES
PATCHES.md
PATCHES.txt

# Exclude all files containing readme information.
README
README.markdown
README.md
README.rdoc
README.rst
README.txt

# Exclude all files containing information on applied updates.
UPDATE
UPDATE.md
UPDATE.txt

# Exclude all files containing usage information.
USAGE
USAGE.md
USAGE.txt

# Exclude all files containing usage information.
TESTING
TESTING.md
TESTING.txt
